let value = 3
let negValue = -value
console.log(negValue) // -3

console.log(2+2) // 4
console.log(2-2) // 0
console.log(2*2)  // 4
console.log(2/2)  // 1
console.log(2**3)  // (2^3) = 2*2*2 = 8
console.log(2%2)  // 0 Remainder

let str1 = "Hello"
let str2 = " Faria"
let str3 = str1 + str2
console.log(str3) // Hello Faria

console.log("1" + 2) // 12
console.log(1 + "2")  // 12 
console.log("1" + 2 + 2)  // 122
console.log(1 + 2 + "2")  // 32

console.log(true) // true
console.log(+true) // 1
// console.log(true+) // error
console.log(+"") // 0


// * Arithmetic Operators
    // + (Addition)
    // - (Subtraction)
    // * (Multiplication)
    // / (Division)
    // % (Modulus - Remainder)
    // ** (Exponentiation)
    // ++ (Increment)
    // -- (Decrement)


   //* Assignment Operators
    //    = (Assignment)
    // += (Add and assign)
    // -= (Subtract and assign)
    // *= (Multiply and assign)
    // /= (Divide and assign)
    // %= (Modulus and assign)
    // **= (Exponentiation and assign)


   // * Comparison Operators
    //    == (Equal to - loose equality)
    // === (Strictly equal to - strict equality)
    // != (Not equal to - loose inequality)
    // !== (Strictly not equal to - strict inequality)
    // > (Greater than)
    // < (Less than)
    // >= (Greater than or equal to)
    // <= (Less than or equal to)


    // * Logical Operators
    //   && (Logical AND)
    // || (Logical OR)
    // ! (Logical NOT)


    // * Bitwise Operators
    //     & (Bitwise AND)
    // | (Bitwise OR)
    // ^ (Bitwise XOR)
    // ~ (Bitwise NOT)
    // << (Left shift)
    // >> (Right shift)
    // >>> (Zero-fill right shift)

    // * Ternary (Conditional) Operator
        // condition ? valueIfTrue : valueIfFalse;


//         Other Operators:
// typeof operator: Returns the type of a variable.
// instanceof operator: Checks if an object is an instance of a particular class.
// Spread (...) operator: Used to expand iterables (like arrays or strings) into individual elements.
// Nullish Coalescing (??) operator: Provides a default value when the operand is null or undefined.
// Optional Chaining (?.) operator: Allows safely accessing nested properties of an object, returning undefined if an intermediate property is null or undefined.

